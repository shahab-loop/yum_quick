import 'package:flutter/material.dart';
import 'package:food_delivery_app/core/utils/size_utils.dart';
import 'package:food_delivery_app/widgets/basescaffold.dart';

class Splash extends StatelessWidget {
  const Splash({super.key});

  @override
  Widget build(BuildContext context) {
    return BaseScaffold(body: Center(child: Column(
      children: [
        // SizedBox(height:178.95.h ,width:202.w ,child:Image.asset(Asset.) ,)
      ],
    ) ,),);
  }
}
